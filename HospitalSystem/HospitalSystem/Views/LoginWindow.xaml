<Window x:Class="HospitalSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему"
        Height="440" Width="380"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border CornerRadius="16" Background="{StaticResource SidebarBrush}" Padding="28">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="6" Opacity="0.35" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,10,0,20">
                <TextBlock Text="Медицинская система"
                           Foreground="White" FontSize="20" FontWeight="SemiBold"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="КОГБУЗ «Куменская ЦРБ»"
                           Foreground="{StaticResource TextOnDarkMutedBrush}" FontSize="12"
                           HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="Логин" Foreground="{StaticResource TextOnDarkMutedBrush}" FontSize="12" Margin="2,0,0,5"/>
                <TextBox x:Name="LoginBox" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,16"/>

                <TextBlock Text="Пароль" Foreground="{StaticResource TextOnDarkMutedBrush}" FontSize="12" Margin="2,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" Margin="0,0,0,20" KeyDown="PasswordBox_KeyDown"/>

                <Button Content="Войти" Style="{StaticResource PrimaryButtonStyle}" Click="Login_Click"/>
            </StackPanel>

            <Button Grid.Row="2" Content="Закрыть" Style="{StaticResource DangerButtonStyle}"
                    Margin="0,20,0,0" Click="Close_Click"/>
        </Grid>
    </Border>
</Window>